---
# Handlers para el rol

- name: reporte_generado
  debug:
    msg: "Reporte generado en {{ report_path }}/{{ report_filename }}"
  # Comentario: Solo debug; en AWX, podría enviar email o integrar con Slack.